// This code was prepared by Stephanie Baker for James Cook University
// Use as starter code for Wi-Fi connectivity

void setup() {
  // Establish serial communications
  Serial.begin(9600);

}

void loop() {
  // WiFi connection - uncomment the following 10 lines when ready to connect!
//  if (WiFi.status() != WL_CONNECTED) {
//    Serial.print("Attempting to connect to WIFI_NETWORK_NAME: ");
//    Serial.println(WIFI_NETWORK_NAME);
//    WiFi.begin(WIFI_NETWORK_NAME, WIFI_PASSWORD);
//    while(WiFi.status() != WL_CONNECTED) {
//      Serial.print("..");
//      delay(5000);
//    }
//    Serial.println("\nConnected.");
//  }

}
